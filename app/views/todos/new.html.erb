<% content_for :title, "New Todo" %>

<div class="flex items-center justify-center bg-gradient-to-br from-slate-50 to-slate-100 py-6">
  <div class="max-w-2xl w-full mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-8">
      <h1 class="text-4xl font-bold text-slate-900 mb-2">Create New Todo</h1>
      <p class="text-slate-600">Add a new task to your todo list</p>
    </div>

    <!-- Form Container -->
    <div class="bg-white rounded-xl shadow-lg border border-slate-200 p-8">
      <%= render "form", todo: @todo %>

      <!-- Back Link -->
      <div class="mt-6 pt-6 border-t border-slate-200">
        <div class="text-center">
          <%= link_to "← Back to Todos", todos_path, 
                class: "text-slate-600 hover:text-slate-700 font-medium text-sm transition-colors inline-flex items-center" %>
        </div>
      </div>
    </div>
  </div>
</div>
